BookDetailView {
    #allScalars
    store {
            #allScalars
        }
    authors {
            #allScalars
        }
}

input BookInput {

    #allScalars(this,TenantAware)

    id(store)

    authors {
        #allScalars(Author)
        -id
    }
}